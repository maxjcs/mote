<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.longcity.modeler.dao.AddCashApplyDao" >
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from add_cash_apply
    where id = #{id}
  </delete>
  <insert id="insert" parameterType="AddCashApply" useGeneratedKeys="true" keyProperty="id">
    insert into add_cash_apply (user_id,alipay_name,money,last_six_order_no,status, create_time)
    values (#{userId},#{alipayName},#{money},#{lastSixOrderNo},#{status}, now())
  </insert>
  <update id="finish" parameterType="AddCashApply" >
    update add_cash_apply
    set 
      status = 2,
      finish_time = now()
    where id = #{id}
  </update>
  
  <select id="queryList" resultType="AddCashApply" parameterType="map" >
    select id, user_id, alipay_name, money, last_six_order_no,status, create_time,finish_time
    from add_cash_apply
    where 1=1 
    <if test="userId!=null">
            AND user_id = #{userId}
    </if>
    <if test="status!=null">
            AND status = #{status}
    </if>
    limit #{start},#{pageSize}
  </select>
  
</mapper>