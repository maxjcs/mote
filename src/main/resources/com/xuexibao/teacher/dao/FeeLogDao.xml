<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.FeeLogDao">
	<insert id="addFeeLog" parameterType="FeeLog">
		insert into
		fee_log(
			money,
			teacher_id,
			description,
			create_time,
			update_time
		)
		values(
					#{money},
					#{teacherId},
					#{description},
					now(),
					now()
		)
	</insert>
</mapper> 