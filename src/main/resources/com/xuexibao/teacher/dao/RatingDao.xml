<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.RatingDao">
	<select id="allRating" resultType="Rating" parameterType="Rating">
		<![CDATA[
			select id,star,name,description,points
			from rating 
			order by star
		]]>
	</select>

	<update id="updateRating" parameterType="Rating">
		update rating 
		set  update_time=now(),
			  <if test="#{name}!=null and #{name}!='' ">
                	name=#{name},
              </if>
			  <if test="#{description}!=null and #{description}!='' ">
                	description=#{description},
              </if>
			  <if test="#{points}!=null and #{points}!='' ">
                	points=#{points}
              </if>
		where id=#{id}
	</update>
</mapper> 