<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.OrgQuestDao">
	<select id="loadOrgQuest" resultType="OrgQuest" parameterType="String">
		<![CDATA[
			select org_id,real_quest_id,create_time,update_time
			from org_quest 
			where real_quest_id=#{value}
		 ]]>
	</select>
	
	<select id="listInsertOrgId" resultType="Integer" >
		select org_id from org_quest where real_quest_id=#{param1} and org_id in 
		 <foreach item="item" index="index" collection="param2" open="(" separator="," close=")">  
		  #{item}  
		 </foreach> 
	</select>
	
	<insert id="batchInsert" parameterType="list">
		insert into org_quest (org_id,real_quest_id,create_time,update_time,subject_id,learn_phase) 
		values
		<foreach collection="list" item="item" index="index"
			separator=",">
			(#{item.orgId},#{item.realQuestId},now(),now(),#{item.subjectId},#{item.learnPhase})
    	</foreach>
	</insert>
</mapper> 