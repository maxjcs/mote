<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.DictDao">
	<select id="listDictByType" parameterType="int" resultType="Dict">
		select id, name, value 
		from dictionary
		where type = #{value} 
		order by sort_no;
	</select>
</mapper> 