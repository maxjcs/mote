<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.ErrorCorrectionDao">
	<insert id="addErrorCorrection" parameterType="ErrorCorrection">
		insert into
		error_correction(
			question_id,
			teacher_id,
			reason,
			reason_type,
			check_status,
			create_time,
			update_time
		)
		values(
					#{questionId},
					#{teacherId},
					#{reason},
					#{reasonType},
					#{checkStatus},
					now(),
					now()
		)
	</insert>
	
	<select id="hasErrorCorrection" resultType="boolean">
		<![CDATA[
			select count(1) from error_correction where teacher_id = #{param1} and question_id = #{param2} limit 1
		]]>
	</select>
	
	<select id="getQuestionCorrection" resultType="ErrorCorrection">
		<![CDATA[
			select * from error_correction where teacher_id = #{param1} and question_id = #{param2} limit 1
		]]>
	</select>
</mapper> 