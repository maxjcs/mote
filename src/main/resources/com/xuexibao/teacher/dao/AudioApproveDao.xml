<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.AudioApproveDao">
	<insert id="addApprove" parameterType="AudioApprove">
		insert into
		audio_approve(
			audio_id,
			approvor,
			approve_time,
			reason,
			status,
			evalution
		)
		values(
					#{audioId},
					#{approvor},
					now(),
					#{reason},
					#{status},
					#{evalution}
		)
	</insert>
	
	<select id="getApproveByAudioId" resultType="AudioApprove">
		select id,audio_id,star,approvor,approve_time,reason,status,evalution from audio_approve where audio_id=#{value} limit 1
	</select>
	
	

	
	 
	
	
</mapper> 