<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuexibao.teacher.dao.PointLogDao">
	<insert id="addPointLog" parameterType="PointLog">
		insert into
		point_log(
			point,
			remain_point,
			teacher_id,
			description,
			create_time,
			update_time
		)
		values(
					#{point},
					#{remainPoint},
					#{teacherId},
					#{description},
					now(),
					now()
		)
	</insert>
</mapper> 